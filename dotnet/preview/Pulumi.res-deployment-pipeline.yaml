config:
  preview-dotnet:resource: res-deployment-pipeline
  # Optional: pipeline host environment and dev/test environment overrides.
  # When unset, dummy GUIDs are used (preview may not fully succeed live without
  # real environment IDs — see README's "Known limitations" section).
  # preview-dotnet:hostEnvironmentId: 00000000-0000-0000-0000-000000000000
  # preview-dotnet:devEnvironmentId: 00000000-0000-0000-0000-000000000000
  # preview-dotnet:testEnvironmentId: 00000000-0000-0000-0000-000000000000
